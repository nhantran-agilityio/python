services:
    web:
        build: .
        command:
            - bash
            - -c
            - |
                python manage.py collectstatic --noinput --clear
                gunicorn studentManagement.wsgi:application -b 0.0.0.0:8000

        volumes:
            - .:/app
        ports:
            - "8000:8000"
